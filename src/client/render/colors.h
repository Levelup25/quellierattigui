#ifndef COLORS_H
#define COLORS_H

#include <SFML/Graphics.hpp>

// resources

namespace uicolor {
const sf::Color cBlue = sf::Color(42, 103, 146);
const sf::Color white = sf::Color(255, 255, 255);
const sf::Color black = sf::Color(0, 0, 0);
const sf::Color grey = sf::Color(100, 100, 100);
const sf::Color snow = sf::Color(240, 240, 240);
}  // namespace uicolor

#endif